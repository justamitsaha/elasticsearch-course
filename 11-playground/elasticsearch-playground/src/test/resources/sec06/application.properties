# SSL bundle configuration
spring.ssl.bundle.jks.elastic-client.truststore.location=classpath:sec06/certs/elastic.truststore.jks
spring.ssl.bundle.jks.elastic-client.truststore.password=changeit

# Elasticsearch configuration. The uris comma separated
spring.elasticsearch.uris=https://localhost:9200
spring.elasticsearch.username=elastic
spring.elasticsearch.password=password123
spring.elasticsearch.rest-client.ssl.bundle=elastic-client
